import {notFound} from 'next/navigation';import {Header,Footer} from '../../components';import {researchPosts} from '../../fleet-content';const formatPublicDate=(date:string)=>new Intl.DateTimeFormat('en-US',{month:'long',day:'numeric',year:'numeric',timeZone:'UTC'}).format(new Date(`${date}T00:00:00Z`));export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}export default async function ResearchArticle({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=researchPosts.find(p=>p.slug===slug);if(!post)notFound();const articleUrl=`https://outsourcingsmallbusinesses.com/research/${post.slug}`;const jsonLd={'@context':'https://schema.org','@type':'Article',headline:post.title,description:post.excerpt,datePublished:post.published,dateModified:post.published,mainEntityOfPage:articleUrl,publisher:{'@type':'Organization',name:'Outsourcing Small Businesses',url:'https://outsourcingsmallbusinesses.com'}};return <><Header/><main className="fleet-main"><article className="section article-shell"><link rel="canonical" href={articleUrl}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><meta property="article:published_time" content={post.published}/><p className="eyebrow">Philippines staffing research</p><h1>{post.title}</h1><p className="lead">{post.excerpt}</p><time dateTime={post.published}>Published {formatPublicDate(post.published)}</time>{post.body.map((x,i)=><p key={i}>{x}</p>)}</article></main><Footer/></>}
