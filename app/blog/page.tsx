import { Header, Footer } from '../components';import { blogPosts, site } from '../data';export default function Blog(){return <><Header/><main className='section'><div className='container'><p className='eyebrow'>{site.brand}</p><h1>Philippines outsourcing guides</h1><p className='lead'>Practical guides for role planning, hiring Filipino talent, onboarding, and provider questions.</p><div className='cards'>{blogPosts.map(p=><a className='card' href={`/blog/${p.slug}`} key={p.slug}><h3>{p.title}</h3><p>{p.excerpt}</p><span className='btn'>Read guide</span></a>)}</div></div></main><Footer/></>}
